# 开发记录

> 本文档为项目开发过程的精简沉淀（原始 AI 编码对话记录因含本机路径，已 gitignore，不随仓库公开）。

## 1. 项目目标

做一个"开箱即用"的公考申论 RAG 问答接口：用户用自然语言提问，系统基于给定的申论材料作答，并返回引用来源。作为 RAG / Agent 原型验证的小型工程示例。

## 2. 技术选型

| 环节 | 选型 | 原因 |
| --- | --- | --- |
| Web 框架 | FastAPI + Uvicorn | 异步、自动生成 Swagger 文档，接口开发效率高 |
| 编排框架 | LangGraph + LangChain | 用有状态图（retrieve → generate）表达 RAG 流程，便于后续扩展节点 |
| 向量库 | ChromaDB（持久化） | 本地文件存储，无需单独部署服务，适合小项目 |
| 嵌入模型 | paraphrase-multilingual-MiniLM-L12-v2 | 本地离线加载，中文效果好，推理不依赖网络 |
| LLM | DeepSeek / OpenAI（OpenAI 兼容接口） | 通过 langchain-openai 统一调用，配置切换即可 |

## 3. 开发过程要点

1. **数据提取**：编写 `extract_gongkao_data.py`，从题库 Markdown 中解析「题目 / 给定材料 / 参考答案 / 答题演示」，清洗后输出 `data/knowledge_base.json`。
2. **知识库构建**：编写 `load_knowledge_base.py`，将 context 与 analysis 分别入库 ChromaDB，重复执行会清空重建，避免脏数据。
3. **RAG 图**：`app/graph.py` 定义 `State`（messages / context / sources），检索节点负责编码与召回，生成节点基于上下文调用 LLM；来源信息从元数据收集并按文件去重。
4. **接口层**：`app/main.py` 启动时加载编译后的图挂载到 `app.state`，`POST /chat` 返回 `{answer, sources}`。
5. **测试**：`test_api.py` 用 `fake_graph` fixture 保证用例离线、确定、不消耗 LLM 配额；覆盖健康检查、正常问答、参数校验、空值处理与性能打印。

## 4. 踩坑与复盘

详见 `问题复盘记录.md`（按「现象 → 根因 → 解决方案」记录），主要问题包括：

- Hugging Face 下载超时 → 配置 `HF_ENDPOINT` 镜像源 + 手动下载本地模型；
- SentenceTransformer 加载报 `Unrecognized processing class` → 补全 `tokenizer.json` 等模型仓库文件；
- 接口测试返回乱码 → PowerShell 终端编码问题，改用 `python -c` 或 VS Code 终端；
- 引用来源为空 → `State` 未定义 `sources` 字段，扩展状态并在检索节点收集元数据透传。

## 5. 验证结果

- 单测：`pytest test_api.py` 5 个用例全部通过；
- 端到端：真实问答跑通（本地模型加载 + ChromaDB 召回 + DeepSeek 生成），示例见 README「效果示例」。