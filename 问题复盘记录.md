# 问题复盘记录

> 本记录基于 RAG 知识库问答项目开发过程中实际遇到并解决的问题，按「现象 → 根因 → 解决方案」结构沉淀，供后续开发与排障参考。

## 问题总览

| 序号 | 问题维度 | 现象 | 根因 | 解决方案 |
| --- | --- | --- | --- | --- |
| 1 | 环境与网络 | Hugging Face 下载超时（WinError 10060） | 国内网络访问 Hugging Face 受限 | 配置 `HF_ENDPOINT` 镜像源；必要时手动下载模型放入本地目录 |
| 2 | 模型加载与缓存 | SentenceTransformer 加载卡住，报 `Unrecognized processing class` | 本地模型文件夹缺少 `tokenizer.json` 等文件 | 补全模型仓库文件，或改用默认缓存机制重新加载 |
| 3 | 接口调试与数据流 | PowerShell 测试接口返回乱码 | 终端编码与 UTF-8 不兼容 | 使用 `python -c` 方式测试，或在 VS Code 终端运行 |
| 4 | 来源引用（Sources） | `retrieve_node` 无法返回文档来源 | `State` 未定义 `sources` 字段，且未从元数据收集 | 扩展 `State` 类型定义，在检索节点收集 `source_file` 并透传至响应 |

---

## 1. 环境与网络问题

### 现象

首次运行 `load_knowledge_base.py` / `app/graph.py` 下载嵌入模型时，长时间无响应后抛出网络异常：

```
WinError 10060 由于连接方在一段时间后没有正确答复或连接的主机没有反应，连接尝试失败
```

### 根因

`huggingface_hub` 默认直连 `huggingface.co`，国内网络环境下直连超时或被限制，导致模型文件无法下载。

### 解决方案

1. **配置镜像源**：在导入 `sentence_transformers` 之前设置环境变量
   ```python
   os.environ["HF_ENDPOINT"] = "https://hf-mirror.com"
   ```
   备选镜像：`https://mirror.sjtu.edu.cn/huggingface`（上海交大）、`https://mirrors.tuna.tsinghua.edu.cn/huggingface`（清华大学）。
2. **手动下载模型**：通过镜像站或第三方渠道下载完整模型目录，放入 `./models/paraphrase-multilingual-MiniLM-L12-v2`，再由 `SentenceTransformer("./models/paraphrase-multilingual-MiniLM-L12-v2")` 本地加载，完全离线运行。

> 复盘要点：镜像源地址可能间歇性不可用（如 `modelscope.cn` 与 Hugging Face Hub 协议不兼容），遇到下载失败时应先确认端点可用性，再切换备用镜像或改为手动放置模型。

---

## 2. 模型加载与缓存问题

### 现象

模型目录已存在，但加载时卡住或直接报错：

```
Unrecognized processing class ... (for tokenizer), while it is expected to be ...
```

### 根因

本地模型目录不完整——缺少 `tokenizer.json`（以及 `tokenizer_config.json` 等必需文件），SentenceTransformer 在初始化 Tokenizer 时无法识别处理类，导致加载中断。

### 解决方案

1. **补全模型仓库文件**：确保目录包含完整文件集（`config.json`、`config_sentence_transformers.json`、`model.safetensors`/`pytorch_model.bin`、`tokenizer.json`、`tokenizer_config.json`、`special_tokens_map.json`、`sentencepiece.bpe.model` 等），缺什么补什么。
2. **使用默认缓存机制**：删除不完整的本地目录，改用
   ```python
   SentenceTransformer("sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2")
   ```
   由库自动下载并复用 `~/.cache/huggingface/hub/` 缓存（配合 `HF_ENDPOINT` 镜像源）。
3. 加载失败时打印完整错误堆栈（`traceback.print_exc()`），根据具体缺失文件精准定位。

> 复盘要点：手动拷贝模型后一定要校验文件完整性（数量与校验和），仅拷贝部分文件的“半套模型”比没有模型更难排查。

---

## 3. 接口调试与数据流问题

### 现象

在 PowerShell 中调用 `/chat` 或直接运行脚本，返回内容里的中文变成乱码（如 `????` 或乱码符号），日志中的中文同样异常。

### 根因

PowerShell 5.1 默认使用系统代码页（GBK）与外部进程交互，而脚本/接口输出为 UTF-8，两者编码不一致导致解码错乱。

### 解决方案

1. **测试方式调整**：改用 `python -c "..."` 或 `@'...'@ | python -` 的方式测试，避免 PowerShell 管道编码转换：
   ```powershell
   $env:PYTHONIOENCODING = 'utf-8'
   python -c "import requests; print(requests.post('http://127.0.0.1:8000/chat', json={'question': '测试'}).text)"
   ```
2. **使用 VS Code 终端**：VS Code 集成终端默认 UTF-8，直接运行 `uvicorn` 与 `curl` 输出正常。
3. **脚本侧兜底**：在入口脚本中主动修正标准输出编码：
   ```python
   if hasattr(sys.stdout, "reconfigure"):
       sys.stdout.reconfigure(encoding="utf-8", errors="replace")
   ```
4. **PowerShell 侧设置**：必要时先执行
   ```powershell
   $OutputEncoding = [System.Text.Encoding]::UTF8
   ```

> 复盘要点：Windows 下“中文乱码”大概率是编码问题而非数据问题，先区分“数据本身错误”与“显示层乱码”，避免误改业务逻辑。

---

## 4. 来源引用（Sources）功能的实现

### 现象

`/chat` 接口能正常返回 `answer`，但 `sources` 始终为空数组，检索命中的文档来源（`source_file`、`question`）无法返回给前端。

### 根因

1. `State`（TypedDict）中只定义了 `messages` 与 `context`，没有 `sources` 字段，检索节点无从写入。
2. `retrieve_node` 仅拼接了 `context` 文本，未从 ChromaDB 返回的 `metadatas` 中收集 `source_file` / `question`。
3. 响应模型 `AnswerResponse.sources` 类型为 `Optional[List[str]]`，无法承载结构化的来源对象；`main.py` 也未读取结果中的来源。

### 解决方案

1. **扩展状态定义**：在 `State` 中新增 `sources: List[dict]`。
2. **检索节点收集来源**：从 `results["metadatas"]` 提取 `source_file` 与 `question`，按文件去重后写入状态：
   ```python
   sources = []
   seen = set()
   for idx, doc in enumerate(documents):
       meta = metadatas[idx] if idx < len(metadatas) and metadatas[idx] else {}
       item = {"source_file": meta.get("source_file", ""), "question": meta.get("question", "")}
       key = (item["source_file"], item["question"])
       if key not in seen:
           seen.add(key)
           sources.append(item)
   return {"context": context, "sources": sources}
   ```
3. **生成节点透传**：`generate_node` 返回时带上 `"sources": state.get("sources", [])`。
4. **响应模型与接口打通**：
   - `AnswerResponse.sources` 改为 `Optional[List[dict]]`；
   - `main.py` 中 `sources = result.get("sources", []) or []`，并填入 `AnswerResponse`。

> 复盘要点：RAG 链路的数据流要一次打通（状态定义 → 检索写入 → 生成透传 → 响应模型 → API 组装），任何一环缺失都会导致“有答案、无来源”的假成功。

---

## 附录：沉淀的通用经验

- 所有密钥与路径一律从 `.env` 读取，禁止硬编码（遵循 `AGENTS.md`）。
- 路径统一基于项目根目录（`BASE_DIR = Path(__file__).resolve().parent.parent`），避免工作目录差异导致加载失败。
- 关键耗时环节（模型加载、编码、向量检索、LLM 调用）添加调试日志与超时控制（如 `timeout=60`），便于快速定位卡点。
- 向量库持久化目录（`./chroma_db`）与知识库文件（`./data/knowledge_base.json`）为生成产物，可随时重建，勿手工改动。